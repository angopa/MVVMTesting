<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/tasks_fragment_dest">
    <!--Landing Page-->
    <fragment
        android:id="@+id/tasks_fragment_dest"
        android:name="com.andgp.mvvmtesting.ui.tasks.TasksFragment"
        android:label="@string/app_name">
        <action
            android:id="@+id/action_tasksFragment_to_statisticsFragment"
            app:destination="@+id/statistics_fragment_dest" />
        <action
            android:id="@+id/action_tasksFragment_to_taskDetailsFragment"
            app:destination="@id/task_detail_fragment_dest" />
        <action
            android:id="@+id/action_tasksFragment_to_addEditTaskFragment"
            app:destination="@+id/add_edit_fragment_dest" />
        <argument
            android:name="userMessage"
            android:defaultValue="0"
            app:argType="integer" />
    </fragment>

    <fragment
        android:id="@+id/task_detail_fragment_dest"
        android:name="com.andgp.mvvmtesting.ui.taskdetail.TaskDetailFragment"
        android:label="Task Details">
        <action
            android:id="@+id/action_taskDetailsFragment_to_addEditTaskFragment"
            app:destination="@+id/add_edit_fragment_dest" />
        <action
            android:id="@+id/action_taskDetailsFragment_to_taskFragment"
            app:destination="@id/tasks_fragment_dest" />
        <argument
            android:name="taskId"
            app:argType="string" />
    </fragment>

    <fragment android:id="@+id/statistics_fragment_dest">

    </fragment>

    <fragment
        android:id="@+id/add_edit_fragment_dest"
        android:name="com.andgp.mvvmtesting.ui.addedittask.AddEditTaskFragment"
        android:label="{title}">
        <action
            android:id="@+id/action_addEditTaskFragment_to_tasksFragment"
            app:destination="@id/tasks_fragment_dest" />
        <argument
            android:name="taskId"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="title"
            app:argType="string"
            app:nullable="false" />
    </fragment>

    <argument
        android:name="userMessage"
        android:defaultValue="0" />

</navigation>